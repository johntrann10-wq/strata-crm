# Baked into production client builds (committed — no secrets).
# Browser calls same-origin `/api/*`; Netlify/Vercel edge proxies to STRATA_API_ORIGIN.
# Override in the host UI: set VITE_API_URL or NEXT_PUBLIC_API_URL for direct API calls instead.
VITE_ALLOW_RELATIVE_API=true
