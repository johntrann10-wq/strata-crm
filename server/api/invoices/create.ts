/**
 * Create invoice API handler
 */

export async function createInvoice(params: Record<string, unknown>) {
  // TODO: validate params, call backend/create record
  return { ok: true, id: null as string | null };
}
