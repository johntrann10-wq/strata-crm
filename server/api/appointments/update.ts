/**
 * Update appointment API handler
 */

export async function updateAppointment(id: string, params: Record<string, unknown>) {
  // TODO: validate id & params, update record
  return { ok: true, id };
}
